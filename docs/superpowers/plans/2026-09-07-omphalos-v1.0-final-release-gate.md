# Omphalos / AUSI Runtime v1.0 Final Release Gate Plan

**Goal:** Promote the already-verified `1.0.0rc1` Runtime to final `1.0.0` only after the canonical G1–G9 gates, four reference workflows, benchmark replay, security checks, clean packaging, and reproducible artifact checks pass on the exact final release head.

**Base:** `master@c291567bcc4ec59bacdc90036819e0db264a2f23` (merged v0.9 RC)

**Branch:** `release/omphalos-v1.0-final-gate`

## Constraints

- No new Search Method, Provider, Planner behavior, Routing behavior, Policy authorization path, Evidence semantics, Stopping semantics, Experience-learning behavior, Evaluation algorithm, or crawler behavior.
- Preserve the historical `1.0.0rc1` release artifacts unchanged.
- Final package version becomes `1.0.0`; public API version remains `1.0`.
- The frozen public facade remains `omphalos`; distribution remains `ai-web-research`.
- `Planning != Authorization`, `UNKNOWN != ALLOW`, `Retrieved != Verified`, `Citation != Support`, `Saturation != CompleteRecall`, `SearchReceipt != ChainOfThought`, and `Learning != SelfAuthorization` remain permanent invariants.
- Final source HEAD and wheel/sdist hashes are recorded externally in CI / PR / FINAL ZIP manifests to avoid self-referential source artifacts.
- Do not merge the final release PR or create the release tag without a separate explicit merge/release instruction.

## Gate Evidence Mapping

- **G1 Method:** `test_method_registry.py`, `test_builtin_method_corpus.py`, `test_method_coverage.py`
- **G2 Provider topology:** `test_omphalos_identity_topology.py`, `test_model_native_providers.py`
- **G3 Replaceability:** `test_provider_substitution_e2e.py`, `test_dynamic_routing.py`
- **G4 Planner:** `test_autonomous_planner_e2e.py`, `test_autonomous_planner_v1.py`
- **G5 Policy:** `test_policy_runtime.py`, `test_policy_runtime_downstream.py`, `test_trusted_execution_runtime.py`
- **G6 Evidence:** `test_evidence_promotion_gate.py`, `test_evidence_provenance_closure_e2e.py`
- **G7 Gap / Stop:** `test_stopping_e2e.py`, `test_search_control_runtime.py`
- **G8 Receipt:** `test_search_receipts.py`, `test_experience_store_replay.py`
- **G9 Evaluation:** `test_evaluation_suite_e2e.py`, `test_evaluation_reproducibility.py`

## Reference Workflow Replay

- General Web: AI Daily live-discovery + candidate/evidence fixture path.
- Current X / Web: Grok X/Web model-native contract tests + General Web acquisition boundary.
- Academic / NPL: Crossref trusted receipt E2E.
- Patent Prior-Art: software prior-art extension workflow + EPO trusted E2E.

These final-gate replays are deterministic credential-free E2E/reference-path tests. They are not presented as live-provider benchmark evidence.

## Tasks

1. **RED final contract** — add final release tests/workflow that require package `1.0.0`, final API artifact, final manifest, G1–G9 evidence map, and four reference-workflow mappings; confirm clean CI is red while package is still RC.
2. **Version promotion** — promote `pyproject.toml` and `src/omphalos/version.py` to `1.0.0`; keep public API version `1.0`.
3. **Final API / manifest** — add `release/omphalos-v1.0.0-public-api.json` and deterministic `release/omphalos-v1.0.0-final-manifest.json`, bound to merged RC SHA and immutable benchmark/doc inputs.
4. **Final gate implementation** — add `src/omphalos/final_release.py` and `scripts/omphalos_final_release_gate.py`; verify package/API identity, historical RC artifact presence, final artifact exact regeneration, G1–G9 evidence files, reference workflows, benchmark artifacts, security scan, and workflow contract.
5. **Docs** — update Quickstart to final `1.0.0`; add final release notes and final checkpoint without embedding a self-referential final sdist hash.
6. **Exact clean CI** — full pytest, compileall, final release gate, explicit G1–G9 replay, explicit four-workflow replay, exact benchmark replay/regeneration, secret scan, build twice, clean wheel install outside source tree.
7. **Single milestone commit** — squash construction history so release branch is `ahead 1 / behind 0` against the merged RC master; rerun the exact final-head workflow.
8. **Final release PR + offline artifact** — open a non-draft final release PR without merging; download exact Actions artifact; create deterministic FINAL candidate ZIP + SHA-256 + manifests; verify clean extraction and package hashes.
9. **Release authorization boundary** — merge/tag only after a separate explicit instruction once the final PR head is verified.

## Exit Rule

The v1.0 Final Release Gate is technically ready only when the exact one-commit release PR head passes all G1–G9, four reference-workflow replay, benchmark replay, full-suite, security, reproducible-build, and clean-install gates. Final publication/tagging remains a separate explicit authorization step.